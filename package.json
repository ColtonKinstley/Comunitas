{
  "name": "comunitas",
  "private": true,
  "type": "module",
  "workspaces": ["api", "web"],
  "scripts": {
    "dev": "bun run --filter api --filter web dev",
    "db:push": "bun run --filter api db:push",
    "db:seed": "bun run --filter api db:seed",
    "db:reset": "bun run --filter api db:reset",
    "typecheck": "bun run --filter api --filter web typecheck"
  }
}
